// import { login } from "../api/login"

export default {
    // vlogin(context, datastr){
    //     return new Promise((resolve, reject) => {
    //         login(datastr).then(res => {
    //             console.log(res.data.username);
    //             context.commit("setUname", res.data.username);
    //             resolve(res.data.token)
    //         })
    //     })
    // }
}